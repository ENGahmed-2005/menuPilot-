/* ==========================================================================
   useAuth.js — إعادة تصدير لتسهيل الاستيراد من مجلد hooks
   --------------------------------------------------------------------------
   المصدر الحقيقي للمنطق هو context/AuthContext.jsx (لازم Provider واحد
   بالتطبيق كله). هذا الملف فقط راحة: import { useAuth } from "../hooks/useAuth"
   بدل ما تتذكر إنه بالـ context.
   ========================================================================== */
export { useAuth } from "../context/AuthContext";
