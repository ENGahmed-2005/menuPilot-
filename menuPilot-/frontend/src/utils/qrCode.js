/* ==========================================================================
   qrCode.js — أدوات مساعدة لبناء روابط QR الخاصة بالطاولات
   يغطي: FR-07 (توليد QR تلقائي لكل طاولة)
   --------------------------------------------------------------------------
   الـ backend هو من يولّد رمز QR فعليًا (كصورة PNG/SVG) ويخزّن tableCode.
   هذه الدالة فقط تبني الرابط اللي يُطبع/يُشفّر داخل رمز QR، ليعرف الفرونت
   إند إلى أي مسار يوجّه الزبون بعد المسح.
   ========================================================================== */
export function buildTableScanUrl(tableCode, baseUrl = window.location.origin) {
  return `${baseUrl}/t/${tableCode}`;
}
